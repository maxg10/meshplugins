{
    "id": "maxg10/lifelines",
    "name": "Lifelines",
    "version": "1.0.2",
    "description": "Finds the single points of failure in your mesh: the nodes that, if they went off the air, would cut other nodes off from the network. Frontend only, no backend, no extra traffic.",
    "author": {
        "name": "Mariusz \"Max\" Gieparda",
        "github": "maxg10",
        "url": "https://meshpulse.app"
    },
    "license": "GPL-3.0",
    "compatibility": {
        "min_mapper_version": "2.8.0",
        "tested_up_to": "2.8.0"
    },
    "permissions": [
        "raw_map_access"
    ],
    "backend": null,
    "frontend": {
        "pages": [],
        "js": "frontend/js/plugin.js",
        "css": "frontend/css/plugin.css"
    },
    "hooks": [],
    "api_routes": [],
    "websocket_channels": [],
    "dependencies": [],
    "config": {
        "default_enabled": {
            "type": "boolean",
            "default": false,
            "description": "Analyse the mesh as soon as the map loads"
        },
        "min_orphans": {
            "type": "number",
            "default": 1,
            "min": 1,
            "max": 100,
            "description": "Only list a node if losing it would cut off at least this many others"
        },
        "include_own_radio": {
            "type": "boolean",
            "default": true,
            "description": "Count the nodes your own radio hears directly as links. Your node is a hub, so it will usually top the list — which is true, but you may want it out of the way."
        },
        "recompute_ms": {
            "type": "number",
            "default": 15000,
            "min": 2000,
            "max": 120000,
            "description": "Shortest time between recomputations when reports keep arriving (ms)"
        }
    }
}
